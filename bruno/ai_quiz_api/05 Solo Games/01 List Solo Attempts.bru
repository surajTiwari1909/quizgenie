meta {
  name: List Solo Attempts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/games/solo
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}

meta {
  name: Finish Contest - Host
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/multiplayer/contests/{{contestId}}/finish
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}

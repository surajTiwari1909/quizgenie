meta {
  name: Get Document
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/documents/{{documentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}

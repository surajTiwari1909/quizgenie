meta {
  name: Delete Document
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/documents/{{documentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
